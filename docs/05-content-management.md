# Управление контентом сайта

В этом разделе описано, где и как редактируется содержимое сайта:

- тексты
- услуги
- статьи
- блог
- ссылки
- блоки главной страницы

Все изменения выполняются в Twig-файлах проекта.  
HTML редактировать не нужно.

---

# Главная страница

Главная страница собирается из Twig-шаблонов:

```
source/layout/main-page/
```

Каждый блок страницы — отдельный файл.

---

## 1. Хедер (верхнее меню) и Первый экран 

Файл:

```
layout/main-page/header/header.twig
```

Здесь редактируются:

- лого
- пункты меню
- заголовок
- подзаголовок
- кнопка

⚠ Важно  
Если добавлять много пунктов в меню — верстка может «поехать» на некоторых или всех разрешениях.  
После изменений нужно проверить сайт на всех разрешениях.

---

## 2. Услуги для физических лиц

Файл блока:

```
layout/main-page/services/services.twig
```

Здесь находятся карточки услуг.

Каждая услуга:


- содержит ссылку
- ведет на отдельную страницу услуги

---

### Где хранятся страницы услуг

```
layout/articles/
```

Каждый файл = отдельная услуга.

---

### Как добавить новую услугу

Блок услуг на главной странице управляется **массивом `services`**.  
Менять нужно только данные в массиве — верстку не трогать.

Пример:

```twig
{% set services = [
  {
    title: 'Услуги семейного юриста',
    icon: 'divorce',
    list: [
      { text: 'оформление развода', href: '/error.html' }
    ]
  }
] %}
```

---

### Добавить новую карточку услуг

1. Скопировать любой объект `{ ... }` в массиве `services`
2. Поменять:
    - `title`
    - `icon`
    - `list`

⚠ Если карточек станет больше — верстка может поехать.  
Проверить главную после изменений.

---

### Добавить услугу в карточку

Внутри `list` добавить элемент:

```twig
{ text: 'Новая услуга', href: '/error.html' }
```

---

### Ссылки на услуги

Если страница услуги есть → ссылка на неё:

```twig
href: '/articles/divorce.html'
```

Если страницы нет → заглушка:

```twig
href: '/error.html'
```

---

### Где лежат страницы услуг

Twig-шаблоны:

```
source/layout/articles/
```

После сборки:

```
source/articles/
```

Редактировать нужно Twig, не HTML.

---

### Иконки

`icon: 'car'` → используется `car.svg`  
Имя должно совпадать с файлом иконки.

---

## 3. Блок «Обо мне»

Файл:

```
layout/main-page/about/about.twig
```

Меняется:

- описание
- количество лет в профессии
- количество выигранных дел

---

## 4. Блок «Цены»

Файл:

```
layout/main-page/prices/prices.twig
```

Меняется:

- таблица с ценами

---

## 5. Карта

Файл:

```
layout/main-page/map/map.twig
```

Сейчас используется картинка.

💡 Рекомендация  
Можно заменить на карту 2GIS. Они предоставляют код, который можно вставить на сайт. 
Сильно лучше работает для локального бизнеса.

---

## 6. Футер

Файл:

```
layout/components/footer/footer.twig
```

Меняется:

- контакты
- соцсети
- адрес
- режим работы

---

# Блог

Блог — это раздел для статей,  
которые **не относятся напрямую к услугам**.

---

## Страница списка блога

Файл:

```
layout/blog/blog.twig
```

Это страница-список всех статей.

Здесь добавляются карточки-превью.

---

### Как добавить статью в блог

1. Создать файл статьи в:

```
layout/blog/
```

2. Добавить карточку в:

```
layout/blog/blog.twig
```

3. Указать ссылку на статью

4. Запустить `gulp`

---

## Страница статьи блога

Файлы:

```
layout/blog/*.twig
```

Каждый файл = отдельная статья.

Меняется:

- заголовок
- текст
- изображения
- блоки

---

# Разница: услуги vs блог

**Статьи услуг**
- связаны с услугами
- ведут с главной
- лежат в `layout/articles`

**Статьи блога**
- отдельный контент
- не привязан к услугам
- лежит в `layout/blog`

---

# Кейсы

Раздел «Кейсы» пока не реализован.

Возможные варианты:

- добавить страницу кейсов
- оформить кейсы как блог-статьи
- удалить пункт из меню

Решение можно принять позже.

---

# Важно

После любых изменений:

```bash
gulp
```

Чтобы опубликовать сайт:

```bash
gulp build
```

Загрузить папку:

```
build/
```

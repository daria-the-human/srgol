# Локальный запуск проекта

---

## 1. Требования

Необходимо установить:

- Git
- Node.js (версия LTS)
- npm (устанавливается вместе с Node.js)

### Проверка установки

Откройте терминал и выполните:

```
bash
git --version
node -v
npm -v
```

Если какая-либо команда не найдена, необходимо установить соответствующий инструмент.

---

## 2. Клонирование репозитория

Перейдите в папку, куда хотите скачать проект:

```bash
cd ~/Projects
```

### Через SSH (рекомендуется)

```bash
git clone git@github.com:daria-the-human/srgol.git
cd <PROJECT_FOLDER>
```

### Через HTTPS

```bash
git clone https://github.com/daria-the-human/srgol.git
cd <PROJECT_FOLDER>
```

---

## 3. Установка зависимостей

Внутри папки проекта выполните:

```bash
npm install
```

Будет создана папка `node_modules/`.

---

## 4. Запуск проекта

Для запуска в режиме разработки выполните:

```bash
gulp
```

После запуска:

- Twig компилируется в HTML
- SCSS компилируется в CSS
- запускается локальный сервер
- изменения отслеживаются автоматически

Если возникает ошибка `gulp: command not found`, выполните:

```bash
npx gulp
```

---

## 5. Где вносить изменения

🚫 HTML-файлы в `source/` вручную не редактируются. 🚫

Изменения вносятся в:

- `source/layout/` — Twig-шаблоны
- `source/scss/` — стили
- `source/js/` — скрипты
- `source/assets/` — изображения и ресурсы

HTML генерируется автоматически.

---

## 6. Production-сборка

Для сборки финальной версии выполните:

```bash
gulp build
```

Готовый сайт будет находиться в папке:

```
build/
```

⬆️ На сервер загружается только содержимое папки `build/`.
